@model Lab4.Paycheck

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Paycheck</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @*<div class="form-group">
                <label asp-for="PersonId" class="control-label"></label>
                <select asp-for="PersonId" class ="form-control" asp-items="ViewBag.PersonId"></select>
            </div>*@
            <div class="form-group">
                <label asp-for="MonthDate" class="control-label"></label>
                <input type="date" asp-for="MonthDate" class="form-control" />
                <span asp-validation-for="MonthDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Salary" class="control-label"></label>
                <input asp-for="Salary" class="form-control" />
                <span asp-validation-for="Salary" class="text-danger"></span>
            </div>

            <input type="hidden" value="@ViewBag.PersonId" name="PersonId" />

            <div class="form-group">
                <input type="submit" value="Створити" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    @Html.ActionLink("Назад до списку чеків", "Index", new { id = @ViewBag.PersonId, name = @ViewBag.PersonName })
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
